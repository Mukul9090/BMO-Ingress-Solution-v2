name: 🚀 Deploy to Kubernetes

on:
  push:
    branches: [main, develop]
  workflow_dispatch:  # Manual trigger

jobs:
  deploy:
    name: Build and Deploy All Clusters
    runs-on: ubuntu-latest
    
    steps:
      - name: 🔧 Set lowercase repository name
        id: repo
        run: |
          REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          echo "repo_lower=$REPO_LOWER" >> $GITHUB_OUTPUT
          echo "Repository (lowercase): $REPO_LOWER"
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Install all dependencies
        run: |
          echo "=== Installing Required Dependencies ==="
          
          # Update package list
          sudo apt-get update -qq
          
          # Install essential build tools and utilities
          sudo apt-get install -y -qq \
            curl \
            wget \
            ca-certificates \
            gnupg \
            lsb-release \
            jq \
            git \
            bash \
            sed \
            grep \
            coreutils
          
          # Verify basic tools
          echo "✓ Basic tools installed"
          bash --version | head -1
          curl --version | head -1
          sed --version | head -1
          
          # Install kubectl
          echo ""
          echo "=== Installing kubectl ==="
          KUBECTL_VERSION=$(curl -L -s https://dl.k8s.io/release/stable.txt)
          curl -LO "https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/amd64/kubectl"
          chmod +x kubectl
          sudo mv kubectl /usr/local/bin/
          kubectl version --client
          echo "✓ kubectl installed"
          
          # Verify all required commands are available
          echo ""
          echo "=== Verifying Dependencies ==="
          command -v bash && echo "✓ bash: $(bash --version | head -1)"
          command -v curl && echo "✓ curl: $(curl --version | head -1)"
          command -v sed && echo "✓ sed: $(sed --version | head -1)"
          command -v base64 && echo "✓ base64: available"
          command -v kubectl && echo "✓ kubectl: $(kubectl version --client --short 2>/dev/null || echo 'installed')"
          command -v jq && echo "✓ jq: $(jq --version)"
          command -v git && echo "✓ git: $(git --version)"
          
          echo ""
          echo "✅ All dependencies installed and verified!"

      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: ✅ Verify Docker and Buildx
        run: |
          echo "=== Verifying Docker Setup ==="
          docker --version
          docker buildx version
          docker buildx ls
          echo "✓ Docker and Buildx ready"

      - name: 🔐 Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏗️ Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ steps.repo.outputs.repo_lower }}/backend-service:latest
            ghcr.io/${{ steps.repo.outputs.repo_lower }}/backend-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 🔥 Deploy to Hot Cluster
        env:
          KUBECONFIG_HOT: ${{ secrets.KUBECONFIG_HOT }}
          IMAGE_TAG: ghcr.io/${{ steps.repo.outputs.repo_lower }}/backend-service:${{ github.sha }}
          CI_CD_MODE: "true"
        run: |
          mkdir -p ~/.kube
          echo "$KUBECONFIG_HOT" | base64 -d > ~/.kube/config-hot
          export KUBECONFIG=~/.kube/config-hot
          bash deploy-cluster-hot.sh

      - name: 💤 Deploy to Standby Cluster
        env:
          KUBECONFIG_STANDBY: ${{ secrets.KUBECONFIG_STANDBY }}
          IMAGE_TAG: ghcr.io/${{ steps.repo.outputs.repo_lower }}/backend-service:${{ github.sha }}
          CI_CD_MODE: "true"
        run: |
          mkdir -p ~/.kube
          echo "$KUBECONFIG_STANDBY" | base64 -d > ~/.kube/config-standby
          export KUBECONFIG=~/.kube/config-standby
          bash deploy-cluster-standby.sh

      - name: 🌐 Deploy Ingress
        env:
          KUBECONFIG_HOT: ${{ secrets.KUBECONFIG_HOT }}
          INGRESS_METHOD: "manifest"
          CI_CD_MODE: "true"
        run: |
          mkdir -p ~/.kube
          echo "$KUBECONFIG_HOT" | base64 -d > ~/.kube/config-hot
          export KUBECONFIG=~/.kube/config-hot
          bash deploy-ingress.sh

      - name: ✅ Deployment Summary
        run: |
          echo "## 🚀 Deployment Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** \`ghcr.io/${{ steps.repo.outputs.repo_lower }}/backend-service:${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Deployed to:**" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Hot Cluster" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Standby Cluster" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Ingress Controller" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
